// Code generated by govalid; DO NOT EDIT.
package multiple_markers

import (
	"errors"
	"github.com/sivchari/govalid/validation/validationhelper"
)

var (
	// ErrNilUser is returned when the User is nil.
	ErrNilUser = errors.New("input User is nil")

	// ErrUserNameRequiredValidation is returned when the UserName is required but not provided.
	ErrUserNameRequiredValidation = errors.New("field UserName is required")

	// ErrUserEmailRequiredValidation is returned when the UserEmail is required but not provided.
	ErrUserEmailRequiredValidation = errors.New("field UserEmail is required")

	// ErrUserEmailEmailValidation is the error returned when the field is not a valid email address.
	ErrUserEmailEmailValidation = errors.New("field UserEmail must be a valid email address")

	// ErrUserAgeGTEValidation is the error returned when the value of the field is less than 18.
	ErrUserAgeGTEValidation = errors.New("field UserAge must be greater than or equal to 18")
)

func ValidateUser(t *User) error {
	if t == nil {
		return ErrNilUser
	}

	if t.Name == "" {
		return ErrUserNameRequiredValidation
	}

	if t.Email == "" {
		return ErrUserEmailRequiredValidation
	}

	if !validationhelper.IsValidEmail(t.Email) {
		return ErrUserEmailEmailValidation
	}

	if !(t.Age >= 18) {
		return ErrUserAgeGTEValidation
	}

	return nil
}
