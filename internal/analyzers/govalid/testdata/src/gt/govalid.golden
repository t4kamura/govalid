// Code generated by govalid; DO NOT EDIT.
package gt

import (
	"errors"
)

var (
	// ErrNilGT is returned when the GT is nil.
	ErrNilGT = errors.New("input GT is nil")

	// ErrGTIntGTValidation is the error returned when the value of the field is less than the 1.
	ErrGTIntGTValidation = errors.New("field GTInt must be greater than 1")

	// ErrGTInt8GTValidation is the error returned when the value of the field is less than the 1.
	ErrGTInt8GTValidation = errors.New("field GTInt8 must be greater than 1")

	// ErrGTInt16GTValidation is the error returned when the value of the field is less than the 1.
	ErrGTInt16GTValidation = errors.New("field GTInt16 must be greater than 1")

	// ErrGTInt32GTValidation is the error returned when the value of the field is less than the 1.
	ErrGTInt32GTValidation = errors.New("field GTInt32 must be greater than 1")

	// ErrGTInt64GTValidation is the error returned when the value of the field is less than the 1.
	ErrGTInt64GTValidation = errors.New("field GTInt64 must be greater than 1")

	// ErrGTFloat32GTValidation is the error returned when the value of the field is less than the 1.
	ErrGTFloat32GTValidation = errors.New("field GTFloat32 must be greater than 1")

	// ErrGTFloat64GTValidation is the error returned when the value of the field is less than the 1.
	ErrGTFloat64GTValidation = errors.New("field GTFloat64 must be greater than 1")

	// ErrGTUintGTValidation is the error returned when the value of the field is less than the 1.
	ErrGTUintGTValidation = errors.New("field GTUint must be greater than 1")

	// ErrGTUint8GTValidation is the error returned when the value of the field is less than the 1.
	ErrGTUint8GTValidation = errors.New("field GTUint8 must be greater than 1")

	// ErrGTUint16GTValidation is the error returned when the value of the field is less than the 1.
	ErrGTUint16GTValidation = errors.New("field GTUint16 must be greater than 1")

	// ErrGTUint32GTValidation is the error returned when the value of the field is less than the 1.
	ErrGTUint32GTValidation = errors.New("field GTUint32 must be greater than 1")

	// ErrGTUint64GTValidation is the error returned when the value of the field is less than the 1.
	ErrGTUint64GTValidation = errors.New("field GTUint64 must be greater than 1")

	// ErrGTUintptrGTValidation is the error returned when the value of the field is less than the 1.
	ErrGTUintptrGTValidation = errors.New("field GTUintptr must be greater than 1")

	// ErrGTComplex64GTValidation is the error returned when the value of the field is less than the 1.
	ErrGTComplex64GTValidation = errors.New("field GTComplex64 must be greater than 1")

	// ErrGTComplex128GTValidation is the error returned when the value of the field is less than the 1.
	ErrGTComplex128GTValidation = errors.New("field GTComplex128 must be greater than 1")
)

func ValidateGT(t *GT) error {
	if t == nil {
		return ErrNilGT
	}

	if !(t.Int > 1) {
		return ErrGTIntGTValidation
	}

	if !(t.Int8 > 1) {
		return ErrGTInt8GTValidation
	}

	if !(t.Int16 > 1) {
		return ErrGTInt16GTValidation
	}

	if !(t.Int32 > 1) {
		return ErrGTInt32GTValidation
	}

	if !(t.Int64 > 1) {
		return ErrGTInt64GTValidation
	}

	if !(t.Float32 > 1) {
		return ErrGTFloat32GTValidation
	}

	if !(t.Float64 > 1) {
		return ErrGTFloat64GTValidation
	}

	if !(t.Uint > 1) {
		return ErrGTUintGTValidation
	}

	if !(t.Uint8 > 1) {
		return ErrGTUint8GTValidation
	}

	if !(t.Uint16 > 1) {
		return ErrGTUint16GTValidation
	}

	if !(t.Uint32 > 1) {
		return ErrGTUint32GTValidation
	}

	if !(t.Uint64 > 1) {
		return ErrGTUint64GTValidation
	}

	if !(t.Uintptr > 1) {
		return ErrGTUintptrGTValidation
	}

	if !(t.Complex64 > 1) {
		return ErrGTComplex64GTValidation
	}

	if !(t.Complex128 > 1) {
		return ErrGTComplex128GTValidation
	}

	{
		t := t.Struct

		if !(t.Int > 1) {
			return ErrGTIntGTValidation
		}

	}

	return nil
}
