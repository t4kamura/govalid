// Code generated by govalid; DO NOT EDIT.
package gt

import (
	"errors"
)

var (
	// ErrNilGT is returned when the GT is nil.
	ErrNilGT = errors.New("input GT is nil")

	// ErrIntGTValidation is the error returned when the value of the field is less than the 1.
	ErrIntGTValidation = errors.New("field Int must be greater than 1")

	// ErrInt8GTValidation is the error returned when the value of the field is less than the 1.
	ErrInt8GTValidation = errors.New("field Int8 must be greater than 1")

	// ErrInt16GTValidation is the error returned when the value of the field is less than the 1.
	ErrInt16GTValidation = errors.New("field Int16 must be greater than 1")

	// ErrInt32GTValidation is the error returned when the value of the field is less than the 1.
	ErrInt32GTValidation = errors.New("field Int32 must be greater than 1")

	// ErrInt64GTValidation is the error returned when the value of the field is less than the 1.
	ErrInt64GTValidation = errors.New("field Int64 must be greater than 1")

	// ErrFloat32GTValidation is the error returned when the value of the field is less than the 1.
	ErrFloat32GTValidation = errors.New("field Float32 must be greater than 1")

	// ErrFloat64GTValidation is the error returned when the value of the field is less than the 1.
	ErrFloat64GTValidation = errors.New("field Float64 must be greater than 1")

	// ErrUintGTValidation is the error returned when the value of the field is less than the 1.
	ErrUintGTValidation = errors.New("field Uint must be greater than 1")

	// ErrUint8GTValidation is the error returned when the value of the field is less than the 1.
	ErrUint8GTValidation = errors.New("field Uint8 must be greater than 1")

	// ErrUint16GTValidation is the error returned when the value of the field is less than the 1.
	ErrUint16GTValidation = errors.New("field Uint16 must be greater than 1")

	// ErrUint32GTValidation is the error returned when the value of the field is less than the 1.
	ErrUint32GTValidation = errors.New("field Uint32 must be greater than 1")

	// ErrUint64GTValidation is the error returned when the value of the field is less than the 1.
	ErrUint64GTValidation = errors.New("field Uint64 must be greater than 1")

	// ErrUintptrGTValidation is the error returned when the value of the field is less than the 1.
	ErrUintptrGTValidation = errors.New("field Uintptr must be greater than 1")

	// ErrComplex64GTValidation is the error returned when the value of the field is less than the 1.
	ErrComplex64GTValidation = errors.New("field Complex64 must be greater than 1")

	// ErrComplex128GTValidation is the error returned when the value of the field is less than the 1.
	ErrComplex128GTValidation = errors.New("field Complex128 must be greater than 1")
)

func ValidateGT(t *GT) error {
	if t == nil {
		return ErrNilGT
	}

	if !(t.Int > 1) {
		return ErrIntGTValidation
	}

	if !(t.Int8 > 1) {
		return ErrInt8GTValidation
	}

	if !(t.Int16 > 1) {
		return ErrInt16GTValidation
	}

	if !(t.Int32 > 1) {
		return ErrInt32GTValidation
	}

	if !(t.Int64 > 1) {
		return ErrInt64GTValidation
	}

	if !(t.Float32 > 1) {
		return ErrFloat32GTValidation
	}

	if !(t.Float64 > 1) {
		return ErrFloat64GTValidation
	}

	if !(t.Uint > 1) {
		return ErrUintGTValidation
	}

	if !(t.Uint8 > 1) {
		return ErrUint8GTValidation
	}

	if !(t.Uint16 > 1) {
		return ErrUint16GTValidation
	}

	if !(t.Uint32 > 1) {
		return ErrUint32GTValidation
	}

	if !(t.Uint64 > 1) {
		return ErrUint64GTValidation
	}

	if !(t.Uintptr > 1) {
		return ErrUintptrGTValidation
	}

	if !(t.Complex64 > 1) {
		return ErrComplex64GTValidation
	}

	if !(t.Complex128 > 1) {
		return ErrComplex128GTValidation
	}

	{
		t := t.Struct

		if !(t.Int > 1) {
			return ErrIntGTValidation
		}

	}

	return nil
}
