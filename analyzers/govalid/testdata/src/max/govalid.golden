// Code generated by govalid; DO NOT EDIT.
package max

import (
	"errors"
)

var (
	// ErrNilMin is returned when the Min is nil.
	ErrNilMin = errors.New("input Min is nil")

	// ErrIntMaxValidation is the error returned when the value of the field is greater than the maximum value.
	ErrIntMaxValidation = errors.New("field Int must be less than 1")

	// ErrInt8MaxValidation is the error returned when the value of the field is greater than the maximum value.
	ErrInt8MaxValidation = errors.New("field Int8 must be less than 1")

	// ErrInt16MaxValidation is the error returned when the value of the field is greater than the maximum value.
	ErrInt16MaxValidation = errors.New("field Int16 must be less than 1")

	// ErrInt32MaxValidation is the error returned when the value of the field is greater than the maximum value.
	ErrInt32MaxValidation = errors.New("field Int32 must be less than 1")

	// ErrInt64MaxValidation is the error returned when the value of the field is greater than the maximum value.
	ErrInt64MaxValidation = errors.New("field Int64 must be less than 1")

	// ErrFloat32MaxValidation is the error returned when the value of the field is greater than the maximum value.
	ErrFloat32MaxValidation = errors.New("field Float32 must be less than 1")

	// ErrFloat64MaxValidation is the error returned when the value of the field is greater than the maximum value.
	ErrFloat64MaxValidation = errors.New("field Float64 must be less than 1")

	// ErrUintMaxValidation is the error returned when the value of the field is greater than the maximum value.
	ErrUintMaxValidation = errors.New("field Uint must be less than 1")

	// ErrUint8MaxValidation is the error returned when the value of the field is greater than the maximum value.
	ErrUint8MaxValidation = errors.New("field Uint8 must be less than 1")

	// ErrUint16MaxValidation is the error returned when the value of the field is greater than the maximum value.
	ErrUint16MaxValidation = errors.New("field Uint16 must be less than 1")

	// ErrUint32MaxValidation is the error returned when the value of the field is greater than the maximum value.
	ErrUint32MaxValidation = errors.New("field Uint32 must be less than 1")

	// ErrUint64MaxValidation is the error returned when the value of the field is greater than the maximum value.
	ErrUint64MaxValidation = errors.New("field Uint64 must be less than 1")

	// ErrUintptrMaxValidation is the error returned when the value of the field is greater than the maximum value.
	ErrUintptrMaxValidation = errors.New("field Uintptr must be less than 1")

	// ErrComplex64MaxValidation is the error returned when the value of the field is greater than the maximum value.
	ErrComplex64MaxValidation = errors.New("field Complex64 must be less than 1")

	// ErrComplex128MaxValidation is the error returned when the value of the field is greater than the maximum value.
	ErrComplex128MaxValidation = errors.New("field Complex128 must be less than 1")
)

func ValidateMin(t *Min) error {
	if t == nil {
		return ErrNilMin
	}

	if t.Int > 1 {
		return ErrIntMaxValidation
	}

	if t.Int8 > 1 {
		return ErrInt8MaxValidation
	}

	if t.Int16 > 1 {
		return ErrInt16MaxValidation
	}

	if t.Int32 > 1 {
		return ErrInt32MaxValidation
	}

	if t.Int64 > 1 {
		return ErrInt64MaxValidation
	}

	if t.Float32 > 1 {
		return ErrFloat32MaxValidation
	}

	if t.Float64 > 1 {
		return ErrFloat64MaxValidation
	}

	if t.Uint > 1 {
		return ErrUintMaxValidation
	}

	if t.Uint8 > 1 {
		return ErrUint8MaxValidation
	}

	if t.Uint16 > 1 {
		return ErrUint16MaxValidation
	}

	if t.Uint32 > 1 {
		return ErrUint32MaxValidation
	}

	if t.Uint64 > 1 {
		return ErrUint64MaxValidation
	}

	if t.Uintptr > 1 {
		return ErrUintptrMaxValidation
	}

	if t.Complex64 > 1 {
		return ErrComplex64MaxValidation
	}

	if t.Complex128 > 1 {
		return ErrComplex128MaxValidation
	}

	{
		t := t.Struct

		if t.Int > 1 {
			return ErrIntMaxValidation
		}

	}

	return nil
}
