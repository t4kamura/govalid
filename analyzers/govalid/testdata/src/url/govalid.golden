// Code generated by govalid; DO NOT EDIT.
package url

import (
	"errors"
	"github.com/sivchari/govalid/validation/validationhelper"
)

var (
	// ErrNilURL is returned when the URL is nil.
	ErrNilURL = errors.New("input URL is nil")

	// ErrWebsiteURLURLValidation is the error returned when the field is not a valid URL.
	ErrWebsiteURLURLValidation = errors.New("field WebsiteURL must be a valid URL")

	// ErrHomepageURLURLValidation is the error returned when the field is not a valid URL.
	ErrHomepageURLURLValidation = errors.New("field HomepageURL must be a valid URL")

	// ErrApiURLURLValidation is the error returned when the field is not a valid URL.
	ErrApiURLURLValidation = errors.New("field ApiURL must be a valid URL")

	// ErrProfileURLURLValidation is the error returned when the field is not a valid URL.
	ErrProfileURLURLValidation = errors.New("field ProfileURL must be a valid URL")

	// ErrDownloadURLURLValidation is the error returned when the field is not a valid URL.
	ErrDownloadURLURLValidation = errors.New("field DownloadURL must be a valid URL")
)

func ValidateURL(t *URL) error {
	if t == nil {
		return ErrNilURL
	}

	if !validationhelper.IsValidURL(t.WebsiteURL) {
		return ErrWebsiteURLURLValidation
	}

	if !validationhelper.IsValidURL(t.HomepageURL) {
		return ErrHomepageURLURLValidation
	}

	if !validationhelper.IsValidURL(t.ApiURL) {
		return ErrApiURLURLValidation
	}

	if !validationhelper.IsValidURL(t.ProfileURL) {
		return ErrProfileURLURLValidation
	}

	if !validationhelper.IsValidURL(t.DownloadURL) {
		return ErrDownloadURLURLValidation
	}

	return nil
}
