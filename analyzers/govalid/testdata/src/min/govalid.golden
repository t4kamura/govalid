// Code generated by govalid; DO NOT EDIT.
package min

import (
	"errors"
)

var (
	// ErrNilMin is returned when the Min is nil.
	ErrNilMin = errors.New("input Min is nil")

	// ErrIntMinValidation is the error returned when the value of the field is less than the minimum value.
	ErrIntMinValidation = errors.New("field Int must be greater than 1")

	// ErrInt8MinValidation is the error returned when the value of the field is less than the minimum value.
	ErrInt8MinValidation = errors.New("field Int8 must be greater than 1")

	// ErrInt16MinValidation is the error returned when the value of the field is less than the minimum value.
	ErrInt16MinValidation = errors.New("field Int16 must be greater than 1")

	// ErrInt32MinValidation is the error returned when the value of the field is less than the minimum value.
	ErrInt32MinValidation = errors.New("field Int32 must be greater than 1")

	// ErrInt64MinValidation is the error returned when the value of the field is less than the minimum value.
	ErrInt64MinValidation = errors.New("field Int64 must be greater than 1")

	// ErrFloat32MinValidation is the error returned when the value of the field is less than the minimum value.
	ErrFloat32MinValidation = errors.New("field Float32 must be greater than 1")

	// ErrFloat64MinValidation is the error returned when the value of the field is less than the minimum value.
	ErrFloat64MinValidation = errors.New("field Float64 must be greater than 1")

	// ErrUintMinValidation is the error returned when the value of the field is less than the minimum value.
	ErrUintMinValidation = errors.New("field Uint must be greater than 1")

	// ErrUint8MinValidation is the error returned when the value of the field is less than the minimum value.
	ErrUint8MinValidation = errors.New("field Uint8 must be greater than 1")

	// ErrUint16MinValidation is the error returned when the value of the field is less than the minimum value.
	ErrUint16MinValidation = errors.New("field Uint16 must be greater than 1")

	// ErrUint32MinValidation is the error returned when the value of the field is less than the minimum value.
	ErrUint32MinValidation = errors.New("field Uint32 must be greater than 1")

	// ErrUint64MinValidation is the error returned when the value of the field is less than the minimum value.
	ErrUint64MinValidation = errors.New("field Uint64 must be greater than 1")

	// ErrUintptrMinValidation is the error returned when the value of the field is less than the minimum value.
	ErrUintptrMinValidation = errors.New("field Uintptr must be greater than 1")

	// ErrComplex64MinValidation is the error returned when the value of the field is less than the minimum value.
	ErrComplex64MinValidation = errors.New("field Complex64 must be greater than 1")

	// ErrComplex128MinValidation is the error returned when the value of the field is less than the minimum value.
	ErrComplex128MinValidation = errors.New("field Complex128 must be greater than 1")
)

func ValidateMin(t *Min) error {
	if t == nil {
		return ErrNilMin
	}

	if t.Int < 1 {
		return ErrIntMinValidation
	}

	if t.Int8 < 1 {
		return ErrInt8MinValidation
	}

	if t.Int16 < 1 {
		return ErrInt16MinValidation
	}

	if t.Int32 < 1 {
		return ErrInt32MinValidation
	}

	if t.Int64 < 1 {
		return ErrInt64MinValidation
	}

	if t.Float32 < 1 {
		return ErrFloat32MinValidation
	}

	if t.Float64 < 1 {
		return ErrFloat64MinValidation
	}

	if t.Uint < 1 {
		return ErrUintMinValidation
	}

	if t.Uint8 < 1 {
		return ErrUint8MinValidation
	}

	if t.Uint16 < 1 {
		return ErrUint16MinValidation
	}

	if t.Uint32 < 1 {
		return ErrUint32MinValidation
	}

	if t.Uint64 < 1 {
		return ErrUint64MinValidation
	}

	if t.Uintptr < 1 {
		return ErrUintptrMinValidation
	}

	if t.Complex64 < 1 {
		return ErrComplex64MinValidation
	}

	if t.Complex128 < 1 {
		return ErrComplex128MinValidation
	}

	{
		t := t.Struct

		if t.Int < 1 {
			return ErrIntMinValidation
		}

	}

	return nil
}
